-- ReelRamp Pro required tables summary.
-- If you are using the Supabase tools from this session, tables were already created here.
-- For your own Supabase project, create these tables with matching columns or use the app admin after setting APIs.

-- Main tables:
-- videos, series, categories, banners, popup_settings, platform_settings, admin_settings,
-- legal_policies, plans, users, subscriptions, payments, watch_history, likes,
-- bookmarks, video_views, support_tickets, promo_campaigns, notifications,
-- promo_events, audit_logs, referrals, wallet_transactions, content_reports,
-- error_logs, help_articles, push_subscriptions, player_events.

-- Important: Keep SUPABASE_SERVICE_ROLE_KEY only in Vercel server-side environment variables.
-- Do not expose service role key in frontend code.
