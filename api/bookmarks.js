import supabase from './_supabase.js';
const cors=res=>{res.setHeader('Access-Control-Allow-Origin','*');res.setHeader('Access-Control-Allow-Methods','GET, POST, PUT, DELETE, OPTIONS');res.setHeader('Access-Control-Allow-Headers','Content-Type, Authorization');};
export default async function handler(req,res){cors(res);if(req.method==='OPTIONS')return res.status(204).end();try{const table='bookmarks';if(req.method==='GET'){const{user_id}=req.query;let q=supabase.from(table).select('*').order('created_at',{ascending:false});if(user_id)q=q.eq('user_id',user_id);const{data,error}=await q;if(error)throw error;return res.status(200).json(data||[])}if(req.method==='POST'){const b=req.body||{};const{data,error}=await supabase.from(table).insert({user_id:b.user_id||'',video_id:Number(b.video_id||0)}).select().single();if(error)throw error;return res.status(201).json(data)}if(req.method==='DELETE'){const{id,user_id,video_id}=req.body||{};let q=supabase.from(table).delete(); if(id)q=q.eq('id',id); else q=q.eq('user_id',user_id).eq('video_id',video_id); const{error}=await q;if(error)throw error;return res.status(200).json({ok:true})}return res.status(405).json({error:'Method not allowed'})}catch(err){console.error('bookmarks api',err);return res.status(500).json({error:err.message})}}
